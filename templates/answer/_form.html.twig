{{ form_start(form) }}
    {{ form_widget(form) }}
    <button id="save_answer" type="submit" class="btn btn-primary">{{ button_label|default('app.button.save'|trans) }}</button>
{{ form_end(form) }}
